name: Build CLI Binaries

on:
  push:
    branches: [main]
    paths:
      - 'BareMetalWeb.CLI/**'
      - '.github/workflows/build-cli.yml'
  workflow_dispatch:

jobs:
  build-cli:
    strategy:
      fail-fast: false
      matrix:
        include:
          - rid: win-x64
            os: windows-latest
            ext: .exe
            cross: false
          - rid: win-x86
            os: windows-latest
            ext: .exe
            cross: false
          - rid: win-arm64
            os: windows-latest
            ext: .exe
            cross: false
          - rid: linux-x64
            os: ubuntu-latest
            ext: ""
            cross: false
          - rid: linux-arm64
            os: ubuntu-latest
            ext: ""
            cross: true
            cross_pkgs: "clang lld gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu"
            objcopy: aarch64-linux-gnu-objcopy
          - rid: linux-arm
            os: ubuntu-latest
            ext: ""
            cross: true
            cross_pkgs: "clang lld gcc-arm-linux-gnueabihf binutils-arm-linux-gnueabihf"
            objcopy: arm-linux-gnueabihf-objcopy

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install cross-compilation toolchain
        if: matrix.cross
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ${{ matrix.cross_pkgs }}

      - name: Publish AOT (native)
        if: ${{ !matrix.cross }}
        run: >
          dotnet publish BareMetalWeb.CLI/BareMetalWeb.CLI.csproj
          --configuration Release
          --runtime ${{ matrix.rid }}
          --self-contained true
          -p:PublishAot=true
          -p:PublishTrimmed=true
          --output ./cli-out/${{ matrix.rid }}

      - name: Publish AOT (cross)
        if: matrix.cross
        run: >
          dotnet publish BareMetalWeb.CLI/BareMetalWeb.CLI.csproj
          --configuration Release
          --runtime ${{ matrix.rid }}
          --self-contained true
          -p:PublishAot=true
          -p:PublishTrimmed=true
          -p:LinkerFlavor=lld
          -p:ObjCopyName=${{ matrix.objcopy }}
          --output ./cli-out/${{ matrix.rid }}

      - name: Rename binary
        shell: bash
        run: |
          mkdir -p ./artifacts
          cp ./cli-out/${{ matrix.rid }}/bmw${{ matrix.ext }} ./artifacts/bmw-${{ matrix.rid }}${{ matrix.ext }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bmw-${{ matrix.rid }}
          path: ./artifacts/bmw-${{ matrix.rid }}${{ matrix.ext }}

  collect:
    needs: build-cli
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./all-binaries
          merge-multiple: true

      - name: List binaries
        run: ls -lh ./all-binaries/

      - name: Upload combined release
        uses: actions/upload-artifact@v4
        with:
          name: bmw-all-platforms
          path: ./all-binaries/
